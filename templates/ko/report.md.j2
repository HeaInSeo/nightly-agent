# 야간 코드 리뷰 리포트: {{ project_name }}

**날짜**: {{ current_date }}
**브랜치**: {{ target_branch }}
**대상 커밋**: {{ target_commit }}
**머지 베이스**: {{ merge_base }}

## 1. 요약
- **변경된 파일 수**: {{ changed_files_count }}
- **Diff 라인 수**: {{ diff_lines_count }}
- **리뷰 단계 상태**: {{ status_review }}
- **패치 생성 단계 상태**: {{ status_fix }}

**한줄 평가**: {{ one_line_summary }}

## 2. 오늘 확인해야 할 주요 이슈 (Top 3)
{% for issue in top_issues %}
1. **{{ issue.title }}** ({{ issue.severity }}) (`{{ issue.target_files | join(', ') }}`)
   - 조치 권고: {{ issue.suggested_action }}
{% else %}
- 아주 좋습니다. 심각한 이슈가 발견되지 않았습니다.
{% endfor %}

## 3. 변경 사항 분류
- **기능 추가/수정**: {{ categorize.features }} 파일
- **리팩토링**: {{ categorize.refactor }} 파일
- **설정/환경**: {{ categorize.config }} 파일
- **테스트**: {{ categorize.tests }} 파일
- **인프라/스크립트**: {{ categorize.infra }} 파일

## 4. 테스트 & 린트 결과
- **빌드 (Build)**: {{ test_results.build }}
- **테스트 (Test)**: {{ test_results.test }}
- **린트 (Lint/Validate)**: {{ test_results.lint }}

## 5. LLM 상세 리뷰 결과
### 의심되는 버그
{{ llm_review.bugs }}

### 회귀(Regression) 위험도
{{ llm_review.regressions }}

### 누락된 테스트
{{ llm_review.missing_tests }}

### 아키텍처/컨벤션 위반 사항
{{ llm_review.architecture }}

### 성능 / 동시성 문제 위험도
{{ llm_review.performance }}
